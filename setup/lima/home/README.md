# Home directory for Lima

## Overview

The Lima template mounts a host filesystem and, by default, this is the users
home directory `~` but this seems risky and potentially problematic.  Using
this directory instead.

